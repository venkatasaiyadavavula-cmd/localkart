// Placeholder — real e2e coverage lives in global-auth-routes.e2e-spec.ts
describe('App (placeholder)', () => {
  it('is covered by global-auth-routes.e2e-spec.ts', () => {
    expect(true).toBe(true);
  });
});
